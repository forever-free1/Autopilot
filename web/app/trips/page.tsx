"use client";import { useState } from "react";import { useQuery } from "@tanstack/react-query";import { BatteryCharging, CarFront, Clock3, MapPin, Navigation, Route, Wrench } from "lucide-react";import {Badge,PageHeader} from "@/components/page-kit";import {createTripPlan,prepareVehicle,TripPlan} from "@/lib/api";
export default function Trips(){const {data:vehicle}=useQuery({queryKey:["vehicle"],queryFn:prepareVehicle});const [plan,setPlan]=useState<TripPlan|null>(null);const [busy,setBusy]=useState(false);async function create(){if(!vehicle)return;setBusy(true);try{setPlan(await createTripPlan(vehicle.id,"上海张江高科技园区","上海虹桥火车站"))}finally{setBusy(false)}}return <><PageHeader title="行程规划" description="结合车辆续航与沿途设施生成可持久化路线建议" action={<button className="button" disabled={busy||!vehicle} onClick={create}><Navigation size={16}/>{busy?"规划中":"生成路线"}</button>}/><div className="trip-layout"><section className="map-canvas"><div className="road r1"/><div className="road r2"/><div className="road r3"/><div className="route-line"/><span className="map-marker start"><CarFront/></span><span className="map-marker charge"><BatteryCharging/></span><span className="map-marker repair"><Wrench/></span><span className="map-marker end"><MapPin/></span><div className="map-key"><span><i className="blue"/>推荐路线</span><span><BatteryCharging/>充电站</span><span><Wrench/>维修站</span></div></section><aside className="route-panel"><h2>路线详情</h2><div className="route-point"><span>A</span><div><small>出发地</small><b>{plan?.origin??"上海张江高科技园区"}</b></div></div><div className="route-point"><span>B</span><div><small>目的地</small><b>{plan?.destination??"上海虹桥火车站"}</b></div></div><div className="route-summary"><div><Route/><b>{plan?.distance_km??"—"} km</b><small>路线距离</small></div><div><Clock3/><b>{plan?.duration_minutes??"—"} 分钟</b><small>预计时间</small></div><div><BatteryCharging/><b>{plan?.energy_percent??"—"}%</b><small>预计耗电</small></div></div><div className="agent-advice"><div><Navigation/><b>Agent 规划建议</b></div><p>{plan?.advice??"点击“生成路线”，Go 服务会结合当前车辆电量生成确定性规划结果。"}</p><p><Badge tone="amber">数据说明</Badge> 第一版路线参数用于离线可复现 Demo，地图供应商适配器将在后续接入。</p></div><h3>沿途设施</h3>{["张江超级充电站 · 1.2 km","中环汽车服务中心 · 8.6 km","虹桥 P9 停车场 · 36.5 km"].map((x,i)=><div className="facility" key={x}><span>{i===0?<BatteryCharging/>:i===1?<Wrench/>:<CarFront/>}</span><div><b>{x}</b><small>{i===0?"推荐补能点":i===1?"维修服务":"目的地停车"}</small></div></div>)}</aside></div></>}
