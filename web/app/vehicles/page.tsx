import Link from "next/link";import { BatteryCharging, CarFront, Plus, Search } from "lucide-react";import { PageHeader,Badge } from "@/components/page-kit";import {vehicles} from "@/lib/demo-data";
export default function Vehicles(){return <><PageHeader title="车辆管理" description="查看已接入车辆的在线状态、位置与健康概况" action={<button className="button"><Plus size={16}/>接入车辆</button>}/><div className="toolbar"><div className="input"><Search size={16}/>搜索 VIN、车牌或车型</div><button className="button secondary">全部状态</button></div><section className="vehicle-grid">{vehicles.map(v=><Link href={`/vehicles/${v.id}`} className="vehicle-card" key={v.id}><div className="vehicle-card-top"><div className="car-visual"><CarFront/></div><Badge tone={v.status==="在线"?"green":"gray"}>{v.status}</Badge></div><h2>{v.model}</h2><p>{v.plate} · {v.vin}</p><div className="vehicle-details"><span><BatteryCharging/>电量 <b>{v.battery}%</b></span><span>续航 <b>{v.range} km</b></span><span>健康 <b>{v.health}</b></span><span>位置 <b>{v.location}</b></span></div></Link>)}</section></>}
