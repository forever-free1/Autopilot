"use client";
import { Suspense } from "react";import { useSearchParams } from "next/navigation";import { useQuery } from "@tanstack/react-query";import { BatteryCharging, CarFront, Gauge, MapPin, Thermometer } from "lucide-react";import {Badge,Metric,PageHeader} from "@/components/page-kit";import {getVehicle} from "@/lib/api";
export default function Page(){return <Suspense fallback={<PageHeader title="车辆详情" description="正在读取车辆数据…"/>}><VehicleDetail/></Suspense>}
function VehicleDetail(){const id=Number(useSearchParams().get("id"));const {data:v,error}=useQuery({queryKey:["vehicle",id],queryFn:()=>getVehicle(id),enabled:Number.isInteger(id)&&id>0});if(!v)return <><PageHeader title="车辆详情" description={error instanceof Error?error.message:"正在读取车辆数据…"}/><section className="panel">请从车辆管理页面选择有效车辆。</section></>;return <><PageHeader title={v.vehicle_model} description={`VIN ${v.vin} · 车辆 ID ${v.id}`} action={<Badge tone="green">在线</Badge>}/><section className="metric-grid four"><Metric label="电池电量" value={`${v.status.battery}%`} meta="实时车辆快照" icon={<BatteryCharging/>}/><Metric label="预计续航" value={`${Math.round(v.status.battery*5.35)} km`} meta="基于当前 SOC 估算" icon={<CarFront/>}/><Metric label="当前速度" value={`${v.status.speed} km/h`} meta={v.status.speed===0?"车辆已驻车":"车辆行驶中"} icon={<Gauge/>}/><Metric label="车内温度" value={`${v.status.temperature}°C`} meta="最新座舱状态" icon={<Thermometer/>}/></section><div className="detail-grid"><section className="panel map-placeholder"><div className="map-pin"><MapPin/></div><div><b>{v.status.location}</b><span>状态来自 Go API 与 Redis 缓存</span></div></section><section className="panel"><div className="panel-head"><div><h2>健康状态</h2><p>当前车辆基础检查</p></div><Badge tone="green">良好</Badge></div>{["动力电池系统","电机与电控","制动系统","低压电气系统"].map(x=><div className="health-row" key={x}><span><i className="green"/>{x}</span><b>正常</b></div>)}</section></div></>}
